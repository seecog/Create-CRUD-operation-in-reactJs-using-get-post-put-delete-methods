import React from 'react';

const Product2 = (props) => {
    return (
        <div>
            <button onClick={props.myClick}>Child Button</button>
            
        </div>
    )
}

export default Product2;